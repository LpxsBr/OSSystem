import { Text, View } from "react-native";

export default function SignIn(params) {
    return (
        <View>
            <Text>
                SignIn screen
            </Text>
        </View>
    )
}